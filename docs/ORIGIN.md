# Origin

This repository was created in Codex as part of the Kinetic Gain portfolio expansion into `Revenue Plumbing`.

It uses the same TypeScript operator-console pattern as nearby portfolio builds, then adapts that pattern to referral-driven B2B waitlist infrastructure.
